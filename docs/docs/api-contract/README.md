# Splitwise API Contract – v1

This directory contains the **authoritative API contract** for the Splitwise Clone project.

## Rules
- API contract is the single source of truth
- Frontend and Backend must strictly follow this
- No breaking change without version bump (`/v2`)

## Base URL
/api/v1

## Authentication
JWT (Bearer Token)

## Response Format
All APIs return a standard response wrapper.
